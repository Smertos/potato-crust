[workspace]
members = ["asset_loader", "client"]
default-members = ["client"]
resolver = "2"


# Enable high optimizations for dependencies (incl. Bevy), but not for our code:
[profile.dev.package."*"]
opt-level = 3
